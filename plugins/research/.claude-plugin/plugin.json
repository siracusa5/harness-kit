{
  "name": "research",
  "description": "Process any source into a structured, compounding knowledge base",
  "version": "0.3.0",
  "category": "research-knowledge",
  "tags": ["research", "knowledge-base", "web-scraping", "pdf", "github", "sources"],
  "requires": {
    "env": [
      {
        "name": "GH_TOKEN",
        "description": "GitHub personal access token — used by gh CLI for repository content fetching",
        "required": false,
        "sensitive": true,
        "when": "Processing GitHub repository URLs"
      }
    ]
  }
}
