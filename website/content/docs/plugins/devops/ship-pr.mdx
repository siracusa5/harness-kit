---
title: ship-pr
---

> **[View in Marketplace →](https://marketplace.harnessprotocol.io/plugins/ship-pr)** for install tracking, related plugins, and profiles.

# ship-pr

End-of-task shipping workflow — run tests, open a PR, code review, fix CI, and squash merge.

## Requirements

- **Claude Code** — required
- **`gh` CLI** ([GitHub CLI](https://cli.github.com/)) — required for PR creation, CI status checks, and merging. `GH_TOKEN` declared as required in `plugin.json` — see [Secrets Management](/docs/guides/secrets-management) for setup.
- **review plugin** — used for automated code review before merge

## Install

```
/plugin marketplace add harnessprotocol/harness-kit
/plugin install ship-pr@harness-kit
```

## What It Does

When you invoke `/ship-pr`:

1. Runs local tests
2. Creates a PR with a structured description (summary, test plan)
3. Runs a code review via subagent — flags MUST FIX issues before proceeding
4. Checks CI — attempts quick fixes (lint, format, typos) if failing
5. Verifies the branch is up-to-date with base
6. Confirms with you, then squash merges and cleans up the branch

## Usage

```
/ship-pr
```

Or use natural language:

```
I'm done with this feature, let's ship it.
Wrap this up and open a PR.
```

## Notes

- Skips drafts — draft PRs are not shipped
- "Just ship it" mode available if you want to skip the confirmation gate
- Uses the `review` skill for code review — install it alongside this plugin

## Runtime Configuration

{/* @embed plugins/ship-pr/skills/ship-pr/SKILL.md */}

## Plugin Documentation

{/* @embed plugins/ship-pr/skills/ship-pr/README.md */}
